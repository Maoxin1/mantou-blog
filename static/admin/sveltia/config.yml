# Sveltia-only overlay. The shared backend and collection definitions live in
# /admin/config.yml so the gray-release CMS cannot drift from the stable admin.
output:
  omit_empty_optional_fields: true
  yaml:
    quote: double
