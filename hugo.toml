title = "mantou の blog"
theme = "LoveIt-0.3.0"
baseURL = "https://mantou-blog.pages.dev/"
languageCode = "zh-cn"
hasCJKLanguage = true
buildFuture = true

# 文章永久地址与中文标题解耦，便于在聊天软件中分享。
[permalinks]
  posts = "/p/:slug/"

[social]
  twitter = "mantouaoao"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[params]
  description = "用真实作品、证据和复盘，把时间转化为能力、资本与自主权。"

# 页面没有 featured image 时使用的稳定站点级分享图（约 1.91:1，适配常见社交卡片）。
[params.seo]
  image = "/images/mantou-social.png"

# 网站左上角显示的站点名（覆盖主题默认的 "My cool site"）
[params.header.title]
  name = "mantou"

[params.home.profile]
  enable = true
  avatarURL = "/images/mantou.jpg"
  title = "mantou"
  subtitle = "把时间转化为能力、资本与自主权。"
  typeit = true
  social = false  # 关闭主页头像下方的社交图标（邮箱 / 推特），更极简

[params.author]
  enable = true
  name = "mantou"
  avatar = "/images/mantou.jpg"
  link = "https://github.com/Maoxin1"

[params.social]
  Email = "2114206091@qq.com"
  twitter = "mantouaoao"

# 统计二选一：已停用 Google Analytics，统一用 Cloudflare Web Analytics
# （脚本见 layouts/partials/analytics.html）。如需恢复 GA，把 enable 改回 true。
[params.analytics]
  enable = false

[params.analytics.google]
  id = "G-0DGXTWGC32"
  anonymizeIP = true

[params.app]
  # 使用 static/ 下生成的标准图标集（favicon.ico / 32 / 16 / apple-touch-icon / site.webmanifest）
  # 不再用照片当 favicon
  title = "mantou の blog"  # 加到主屏/启动器时的应用名（覆盖主题默认的 "My cool site"）
  iconColor = ""  # 置空以禁用主题默认的 mask-icon（safari-pinned-tab.svg 不存在，避免 404）

[params.page]
  lightgallery = true

# 关注入口：follow.it 从本站 RSS 自动读取新文章并发送邮箱提醒。
# 此处仅保存公开的订阅表单地址，不包含账户凭据或 API 密钥。
[params.follow]
  followitAction = "https://api.follow.it/subscription-form/eVdkdTJYM2plUTN1UzFvVkdnUlFsVHRkclJVWTBhQXJZSHllNW5kb3RJbUtSbEI1UWN4bkp0SE1MUDBFbWFFY0RFU2g2WUh0cmtUV2l6VzlOdC9KRThIWDFlUFh3K2JLcVhRdWNRL3RiMVR2MG44YWF6RE5qb0hFbkwvVWcveHl8WVFZQzNaaXhxRm95bzArY3VVVzVlTDB0MUtkTy9keWpFN1k4N3R3RWhmRT0=/8"

# 站内搜索改用 Pagefind（页面见 /search/，需配套 Cloudflare 构建命令）；停用主题自带 Lunr
[params.search]
  enable = false

# 关闭主题默认的 jsdelivr CDN，改用本地打包的库文件
# （jsdelivr 在中国大陆常被墙，会导致 lunr 搜索核心、FontAwesome 等加载失败）
[params.cdn]
  data = ""

# 顶部导航：结构化作品优先，历史长文和日记继续保留。
[menu]
  [[menu.main]]
    identifier = "portfolio"
    name = "作品集"
    url = "/works/"
    weight = 1
  [[menu.main]]
    identifier = "thinking"
    name = "方法与思考"
    url = "/categories/works/"
    weight = 2
  [[menu.main]]
    identifier = "journal"
    name = "日记档案"
    url = "/categories/essays/"
    weight = 4
  [[menu.main]]
    identifier = "about"
    name = "关于与合作"
    url = "/about/"
    weight = 3
  [[menu.main]]
    identifier = "search"
    name = "搜索"
    url = "/search/"
    weight = 5
  [[menu.main]]
    identifier = "follow"
    name = "关注"
    url = "/follow/"
    weight = 6
